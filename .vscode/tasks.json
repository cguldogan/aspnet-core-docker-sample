{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/docker-app.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}